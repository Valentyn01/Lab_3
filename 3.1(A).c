#include <stdio.h>
#include <math.h>
int main()
{
float x,t,c;
printf("введіть точку x=");
scanf("%g",&x);
if(x>0){
t=pow(x,2)+5*x-6;
printf("t="); 
printf("%g", t); 
}
if(x<=0)

{
    t=fabs(x+cos(x));
    if (c!=x>0 && c!=x<=0)
 
    printf("Помилка");
}
    return 0;
}